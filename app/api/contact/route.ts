import {NextResponse} from 'next/server';import {enquirySchema} from '@/lib/validation';import {getSupabaseAdmin} from '@/lib/supabase';
export async function POST(request:Request){try{const parsed=enquirySchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:'Please check the form fields.'},{status:400});const {website,...enquiry}=parsed.data;if(website)return NextResponse.json({ok:true});const db=getSupabaseAdmin();if(db){const {error}=await db.from('enquiries').insert({full_name:enquiry.fullName,company:enquiry.company,email:enquiry.email,phone:enquiry.phone,project_type:enquiry.projectType,project_location:enquiry.projectLocation,project_description:enquiry.projectDescription,preferred_contact_method:enquiry.preferredContactMethod,consent:enquiry.consent});if(error)console.error('Supabase enquiry save failed',error.message)}if(process.env.WEBHOOK_CONTACT_URL)await fetch(process.env.WEBHOOK_CONTACT_URL,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(enquiry)});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Unable to send your enquiry right now.'},{status:500})}}
