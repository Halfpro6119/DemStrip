import type {MetadataRoute} from 'next';import {services,projects} from '@/data/siteData';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://demstrip.co.uk';return ['','/about','/contact','/projects','/services','/health-safety',...services.map(s=>`/services/${s.slug}`),...projects.map(p=>`/projects/${p.slug}`)].map(url=>({url:`${base}${url}`,lastModified:new Date()}))}
