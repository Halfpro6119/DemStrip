import {notFound} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';import {Section} from '@/components/ui/Section';import {projects} from '@/data/siteData';export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}export default function Project({params}:{params:{slug:string}}){const project=projects.find(p=>p.slug===params.slug);if(!project)return notFound();return <><section className="relative pt-20"><Image src={project.image} alt="Development placeholder" width={1600} height={900} className="h-[55vh] w-full object-cover opacity-60"/><Section className="relative -mt-24 bg-coal"><p className="eyebrow">{project.category} / {project.location}</p><h1 className="heading mt-5 text-5xl uppercase lg:text-7xl">{project.title}</h1></Section></section><Section className="grid gap-10 md:grid-cols-2"><div><p className="eyebrow">Overview</p><p className="mt-5 text-lg leading-8 text-white/70">{project.description}</p></div><div className="border-l border-amber pl-6"><p className="eyebrow">Information status</p><p className="mt-4 leading-7 text-white/60">Project scope, client, imagery and detailed case-study information are awaiting confirmation. This page is structured for client-supplied content.</p></div></Section><Section className="border-t border-white/10"><Link href="/contact" className="bg-amber px-6 py-4 text-xs font-extrabold tracking-widest text-coal">DISCUSS YOUR PROJECT</Link></Section></>}
