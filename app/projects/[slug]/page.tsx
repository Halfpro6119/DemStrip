import {notFound} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';import {Section} from '@/components/ui/Section';import {projects} from '@/data/siteData';export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}export default function Project({params}:{params:{slug:string}}){const project=projects.find(p=>p.slug===params.slug);if(!project)return notFound();return <><section className="relative pt-20"><Image src={project.image} alt="DemStrip demolition work" width={1600} height={900} sizes="100vw" quality={90} className="h-[58vh] w-full object-cover"/><div className="absolute inset-x-0 bottom-0 h-2/3 bg-gradient-to-t from-coal to-transparent"/><div className="absolute inset-x-0 bottom-0 mx-auto max-w-[1440px] px-5 pb-10 lg:px-10"><p className="eyebrow">{project.category} / {project.location}</p><h1 className="heading mt-4 text-5xl uppercase lg:text-7xl">{project.title}</h1></div></section><Section className="grid gap-12 lg:grid-cols-[1.2fr_.8fr]"><div><p className="eyebrow">Project overview</p><p className="mt-5 max-w-2xl text-xl leading-8 text-white/70">{project.description}</p></div><div className="border-l border-amber pl-6"><p className="eyebrow">Project information</p><p className="mt-4 leading-7 text-white/60">A considered approach to demolition and clearance, planned around the requirements of each site.</p></div></Section><Section className="border-t border-white/10"><p className="eyebrow">Project gallery</p><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{project.gallery.map((image,index)=><div className="relative aspect-square overflow-hidden border border-white/10" key={image}><Image src={image} alt={`DemStrip project photograph ${index+1}`} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" quality={88} className="object-cover transition duration-500 hover:scale-105"/></div>)}</div></Section><Section className="border-t border-white/10"><Link href="/contact" className="bg-amber px-6 py-4 text-xs font-extrabold tracking-widest text-coal">DISCUSS YOUR PROJECT</Link></Section></>}
