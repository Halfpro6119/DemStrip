import Image from 'next/image';import Link from 'next/link';import {PageHero} from '@/components/ui/PageHero';import {Section} from '@/components/ui/Section';import {projects} from '@/data/siteData';export const metadata={title:'Projects'};export default function Projects(){return <><PageHero eyebrow="Selected work" title="Proven on site." copy="A selection of demolition, strip-out and clearance projects delivered by DemStrip."/><Section><div className="grid gap-8">{projects.map(p=><Link key={p.slug} href={`/projects/${p.slug}`} className="group overflow-hidden border border-white/10 bg-steel"><Image src={p.image} alt="Development placeholder" width={1600} height={900} className="h-80 w-full object-cover opacity-70 transition group-hover:scale-[1.02]"/><div className="p-7"><p className="eyebrow">{p.category}</p><h2 className="heading mt-3 text-4xl uppercase">{p.title}</h2><p className="mt-2 text-white/60">{p.location}</p></div></Link>)}</div></Section></>}
