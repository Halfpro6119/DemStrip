import Image from 'next/image';import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import {PageHero} from '@/components/ui/PageHero';import {Section} from '@/components/ui/Section';import {projects} from '@/data/siteData';export const metadata={title:'Projects'};export default function Projects(){return <><PageHero eyebrow="Selected work" title="Proven on site." copy="A selection of demolition, strip-out and clearance projects delivered by DemStrip."/><Section><div className="mb-12 grid gap-8 border-b border-white/10 pb-10 lg:grid-cols-2"><p className="text-xl leading-8 text-white/70">A project-led approach, structured around the requirements of the individual site.</p><p className="text-sm leading-7 text-white/50 lg:pt-2">Case-study details are published only once project names, imagery and scope have been approved by DemStrip.</p></div><div className="grid gap-8">{projects.map(p=><Link key={p.slug} href={`/projects/${p.slug}`} className="group grid overflow-hidden border border-white/10 bg-steel lg:grid-cols-[1.25fr_.75fr]"><div className="relative min-h-[360px] overflow-hidden"><Image src={p.image} alt="DemStrip demolition work" fill sizes="(max-width: 1024px) 100vw, 63vw" quality={88} className="object-cover opacity-70 transition duration-700 group-hover:scale-[1.04]"/><div className="absolute inset-0 bg-gradient-to-t from-coal/75 to-transparent"/></div><div className="flex flex-col p-8"><p className="eyebrow">{p.category}</p><h2 className="heading mt-5 text-4xl uppercase">{p.title}</h2><p className="mt-3 text-white/60">{p.location}</p><p className="mt-8 text-sm leading-7 text-white/60">{p.description}</p><span className="mt-auto flex items-center gap-2 pt-12 text-xs font-bold tracking-widest text-amber">VIEW PROJECT <ArrowUpRight size={16}/></span></div></Link>)}</div></Section></>}
