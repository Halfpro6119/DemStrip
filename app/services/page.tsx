import Link from 'next/link';import {PageHero} from '@/components/ui/PageHero';import {Section} from '@/components/ui/Section';import {services} from '@/data/siteData';export const metadata={title:'Services'};export default function Services(){return <><PageHero eyebrow="Capability" title="Specialist services." copy="Demolition, deconstruction and clearance services delivered with a professional approach."/><Section><div className="grid gap-px bg-white/10 md:grid-cols-2">{services.map(s=><Link className="bg-coal p-8 hover:bg-steel" key={s.slug} href={`/services/${s.slug}`}><p className="eyebrow">{s.number}</p><h2 className="heading mt-14 text-4xl uppercase">{s.title}</h2><p className="mt-5 max-w-md leading-7 text-white/65">{s.summary}</p><p className="mt-8 text-sm font-bold text-amber">VIEW SERVICE →</p></Link>)}</div></Section></>}
