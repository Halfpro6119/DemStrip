import Link from 'next/link';import {ArrowDownRight} from 'lucide-react';import {PageHero} from '@/components/ui/PageHero';import {Section} from '@/components/ui/Section';import {services} from '@/data/siteData';import {InteractiveCard} from '@/components/ui/InteractiveCard';export const metadata={title:'Services'};export default function Services(){return <><PageHero eyebrow="Capability" title="Specialist services." copy="Demolition, deconstruction and clearance services delivered with a professional approach."/><Section><div className="mb-12 flex flex-wrap items-end justify-between gap-5"><p className="max-w-xl text-lg leading-8 text-white/65">Our services are planned around each site, its constraints and its next phase. Talk to the team about the work you need delivered.</p><Link href="/contact" className="border-b border-amber pb-2 text-sm font-bold text-amber">REQUEST A QUOTE</Link></div><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">{services.map(s=><InteractiveCard className="service-card group relative flex min-h-[330px] flex-col overflow-hidden border border-white/10 p-7 transition hover:border-amber" key={s.slug} href={`/services/${s.slug}`}><p className="text-xs font-bold tracking-[.18em] text-amber">{s.number} / SERVICE</p><p className="absolute right-6 top-3 heading text-7xl text-white/[.035]">{s.number}</p><h2 className="heading mt-auto text-4xl uppercase">{s.title}</h2><p className="mt-5 max-w-md text-sm leading-6 text-white/65">{s.summary}</p><span className="mt-8 flex items-center gap-2 text-xs font-bold tracking-widest">VIEW SERVICE <ArrowDownRight size={16} className="text-amber transition group-hover:translate-x-1 group-hover:translate-y-1"/></span></InteractiveCard>)}</div></Section></>}
