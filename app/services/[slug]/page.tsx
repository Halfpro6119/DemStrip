import {notFound} from 'next/navigation';import Link from 'next/link';import {ArrowLeft} from 'lucide-react';import {PageHero} from '@/components/ui/PageHero';import {Section} from '@/components/ui/Section';import {services} from '@/data/siteData';export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export default function Service({params}:{params:{slug:string}}){const service=services.find(s=>s.slug===params.slug);if(!service)return notFound();return <><PageHero eyebrow={`${service.number} / Service`} title={service.title} copy={service.summary}/><Section className="pb-0"><Link href="/services" className="inline-flex items-center gap-2 text-xs font-bold tracking-[.15em] text-white/60 transition hover:-translate-x-1 hover:text-amber"><ArrowLeft size={16}/> BACK TO SERVICES</Link></Section><Section className="grid gap-12 lg:grid-cols-[1.2fr_.8fr]"><div><p className="eyebrow">What this involves</p><h2 className="heading mt-5 text-4xl uppercase">A considered approach for every site.</h2><p className="mt-7 max-w-2xl text-lg leading-8 text-white/70">{service.detail}</p><p className="mt-6 max-w-2xl leading-7 text-white/60">The team will discuss requirements, site conditions and the appropriate scope before work is planned. Health and safety considerations are central to this process.</p></div><aside className="border border-white/15 bg-steel p-7"><p className="eyebrow">Suitable for</p><ul className="mt-6 grid gap-4 text-lg">{service.suitable.map(x=><li key={x} className="border-b border-white/10 pb-4">{x}</li>)}</ul></aside></Section><Section className="border-t border-white/10"><p className="eyebrow">Discuss your requirements</p><h2 className="heading mt-5 text-5xl uppercase">Plan with confidence.</h2><Link className="mt-8 inline-block bg-amber px-6 py-4 text-xs font-extrabold tracking-widest text-coal" href="/contact">REQUEST A QUOTE</Link></Section></>}
